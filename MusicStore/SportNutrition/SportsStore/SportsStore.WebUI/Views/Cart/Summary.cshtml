@model SportsStore.Domain.Entities.Cart

<div id="cart">
    <span class="caption">
        <b>Корзина:</b>
        @Model.Lines.Sum(x => x.Quantity) товар(ов),            
        @Model.ComputeTotalValue().ToString("c")
    </span>
        
    @Html.ActionLink("Проверить", "Index", "Cart", 
        new { returnUrl = Request.Url.PathAndQuery }, null)  
</div>
